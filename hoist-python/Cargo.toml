[package]
name = "hoist-python"
version = "0.1.0"
edition = "2021"
description = "Python bindings for the Hoist DSL"

[lib]
name = "hoist"
crate-type = ["cdylib"]

[dependencies]
hoist-core = { path = "../hoist-core" }
pyo3 = { version = "0.27", features = ["extension-module"] }
serde_json = "1"
