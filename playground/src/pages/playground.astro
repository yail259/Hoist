---
import Base from "../layouts/Base.astro";
import Editor from "../components/Editor";
---

<Base title="Hoist Playground" description="Try Hoist in your browser">
  <main>
    <nav>
      <a href="/" class="logo">Hoist</a>
      <div class="nav-links">
        <a href="/playground" class="active">Playground</a>
        <a href="https://github.com/anthropics/hoist" target="_blank">GitHub</a>
      </div>
    </nav>

    <div class="playground-header">
      <h1>Playground</h1>
      <p>Run Hoist programs in your browser via WebAssembly.</p>
    </div>

    <Editor client:load />

    <div class="info">
      <p>
        <strong>Pure programs</strong> run entirely in your browser via WebAssembly — no backend needed.
      </p>
      <p>
        <strong>LLM programs</strong> support three modes (selectable in the toolbar):
      </p>
      <ul>
        <li><strong>Simulated LLM</strong> — pre-computed golden responses, instant results</li>
        <li><strong>Real LLM</strong> — runs SmolLM2-135M in your browser via <code>wllama</code> (~105MB one-time download)</li>
        <li><strong>Manual</strong> — type your own responses via <code>window.prompt()</code></li>
      </ul>
    </div>
  </main>
</Base>

<style>
  main {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 0;
    border-bottom: 1px solid var(--border);
  }

  .logo {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text);
    display: inline-flex;
    align-items: center;
    min-height: 44px;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
  }

  .nav-links a {
    color: var(--text-muted);
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    min-height: 44px;
  }

  .nav-links a:hover,
  .nav-links a.active {
    color: var(--text);
    text-decoration: none;
  }

  .playground-header {
    padding: 1.5rem 0 1rem;
  }

  .playground-header h1 {
    font-size: 1.5rem;
    margin-bottom: 0.25rem;
  }

  .playground-header p {
    color: var(--text-muted);
    font-size: 0.95rem;
  }

  .info {
    padding: 1rem 0 2rem;
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .info p {
    margin-bottom: 0.5rem;
  }

  .info code {
    background: var(--bg-tertiary);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.8rem;
  }
</style>

<style is:global>
  /* Editor component styles (need to be global for Preact island) */
  .editor-container {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 500px;
  }

  .toolbar {
    display: flex;
    gap: 0.75rem;
    padding: 0.75rem 0;
    flex-wrap: wrap;
  }

  .toolbar select {
    flex: 1;
    max-width: 300px;
    padding: 0.5rem 0.75rem;
    min-height: 44px;
    font-size: 0.9rem;
    font-family: var(--font-sans);
    background: var(--bg-secondary);
    color: var(--text);
    border: 1px solid var(--border);
    border-radius: 6px;
    cursor: pointer;
  }

  .toolbar select.llm-mode-select {
    flex: 0;
    max-width: 200px;
    font-size: 0.8rem;
    border-color: var(--accent);
  }

  .toolbar select:focus {
    outline: none;
    border-color: var(--accent);
  }

  .wllama-status {
    padding: 0.4rem 0.75rem;
    font-size: 0.8rem;
    font-family: var(--font-mono);
    color: var(--accent-light);
    background: var(--bg-tertiary);
    border: 1px solid var(--border);
    border-radius: 6px;
  }

  .toolbar button {
    padding: 0.5rem 1.25rem;
    min-height: 44px;
    font-size: 0.9rem;
    font-family: var(--font-sans);
    font-weight: 600;
    background: var(--accent);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    transition: background 0.15s;
  }

  .toolbar button:hover:not(:disabled) {
    background: var(--accent-light);
  }

  .toolbar button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .shortcut-hint {
    font-size: 0.75rem;
    color: var(--text-muted);
    align-self: center;
    font-family: var(--font-mono);
    white-space: nowrap;
  }

  @media (max-width: 480px) {
    .shortcut-hint {
      display: none;
    }
  }

  .editor-wrapper {
    flex: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    min-height: 400px;
  }

  @media (max-width: 768px) {
    .editor-wrapper {
      grid-template-columns: 1fr;
    }
  }

  .pane {
    display: flex;
    flex-direction: column;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 8px;
    overflow: hidden;
  }

  .pane-header {
    padding: 0.5rem 1rem;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    background: var(--bg-tertiary);
    border-bottom: 1px solid var(--border);
  }

  .editor-content {
    flex: 1;
    overflow: auto;
  }

  .editor-content .cm-editor {
    height: 100%;
  }

  .output-content {
    flex: 1;
    margin: 0;
    padding: 1rem;
    font-family: var(--font-mono);
    font-size: 0.9rem;
    line-height: 1.6;
    white-space: pre-wrap;
    word-break: break-word;
    overflow: auto;
    color: var(--text-muted);
  }

  .output-content.error {
    color: #ef4444;
  }
</style>
